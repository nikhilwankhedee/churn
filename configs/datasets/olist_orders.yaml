# ======================================================================
# OLIST_ORDERS — Dataset Manifest
# ======================================================================
# Ecosystem: transactional_marketplace
# Churn window: 180 days
# Generated by: churn register / churn wizard
# 
adapter:
  type: generic
  plugin: null

root_directory: null

dataset:
  name: olist_orders
  ecosystem_type: transactional_marketplace

churn:
  strategy: inactivity
  uses_native_churn_label: false
  prediction_window_days: 180
  justification: >
    180-day inactivity window for transactional marketplace.

features:
  available_groups:
    - purchase
    - inactivity

schema:
  version: 2
  column_mapping:
    customer_id: customer_id
    order_purchase_timestamp: event_time
  synthetic_columns: {}

files:
  required:
    data: customer_id
  optional: {}

preprocessing:
  timestamp_columns:
    - order_purchase_timestamp
